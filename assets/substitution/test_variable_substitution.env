PORT=8080
NORMAL=localhost:$PORT
NORMAL_IN_PARENTHESIS=localhost:${PORT}

UNSET=localhost:${UNKNOWN}
UNSET_WITH_DEFAULT=localhost:${UNKNOWN-42}

BLANK=
BLANK_VARIABLE_WITH_DEFAULT=localhost:${BLANK:-42}

ESCAPED_SUBSTITUTION=localhost:\${PORT}

SUBSTRING_EXPANSION=localhost:${PORT:1:3}
SUBSTRING_EXPANSION_FOR_UNKNOWN=localhost:${UNKNOWN:1:3}

NESTED_KEY_REPLACE=localhost:${PORT-$PORT}
NESTED_KEY_REPLACE_UNKNOWN=localhost:${UNKNOWN-$PORT}
